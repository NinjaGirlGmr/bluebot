health_monitor:
  ros__parameters:
    publish_rate_hz: 1.0
    topic_grace_period_sec: 10.0
    node_grace_period_sec: 20.0
    hardware_id: robot_bringup_observability

    topic_names:
      - /rosout
      - /diagnostics
    topic_types:
      - rcl_interfaces/msg/Log
      - diagnostic_msgs/msg/DiagnosticArray
    topic_timeouts_sec:
      - 5.0
      - 3.0

    expected_nodes:
      - health_monitor
      - foxglove_bridge
